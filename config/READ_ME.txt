--- Configuration du site ---

- Connectez-vous sur gitlab
- Faites un clone du repository (via HTTPS dans PhPStorm), dans la racine 'C:\Progs\wamp64\www\Projet_Web\groupe61'
  sinon changez le chemin dans le '.htaccess' du folder 'config'.
- Lancez wampserver (64 bits)
- Créez une nouvelle base de données avec le nom 'project' (cf. 'config.ini')
- Importez le script 'project.sql'
- Importez le script 'project_data.sql'

Remarques:
    - Admin = 'antoine.honinckx' (tous les mdp sont 'azerty')
    - Seul les membres 'yusuf.yilmaz' et 'antoine.honinckx' ont des questions et réponses